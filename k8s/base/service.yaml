apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness
  namespace: aceest
spec:
  type: LoadBalancer
  selector:
    app: aceest-fitness
    track: stable
  ports:
    - name: http
      port: 80
      targetPort: 8000

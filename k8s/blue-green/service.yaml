apiVersion: v1
kind: Service
metadata:
  name: aceest-blue-green
  namespace: aceest
spec:
  type: LoadBalancer
  selector:
    app: aceest-fitness
    color: blue
  ports:
    - name: http
      port: 80
      targetPort: 8000

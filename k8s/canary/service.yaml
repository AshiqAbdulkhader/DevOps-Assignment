apiVersion: v1
kind: Service
metadata:
  name: aceest-canary
  namespace: aceest
spec:
  type: LoadBalancer
  selector:
    app: aceest-canary
  ports:
    - name: http
      port: 80
      targetPort: 8000

apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-stable
  namespace: aceest
spec:
  replicas: 4
  selector:
    matchLabels:
      app: aceest-canary
      release: stable
  template:
    metadata:
      labels:
        app: aceest-canary
        release: stable
        version: v1
    spec:
      containers:
        - name: aceest-fitness
          image: docker.io/ashiqabdulkhader/aceest-fitness:v1
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: aceest-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10

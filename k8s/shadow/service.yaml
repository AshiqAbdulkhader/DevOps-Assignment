apiVersion: v1
kind: Service
metadata:
  name: aceest-shadow
  namespace: aceest
spec:
  type: ClusterIP
  selector:
    app: aceest-shadow
  ports:
    - name: http
      port: 80
      targetPort: 8000
